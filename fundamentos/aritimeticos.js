const [a, v, b, s] = [2,3,4,1]
const soma = a + v + b + s
const subtracao = b - v
const multiplicacao = a * b
const divisao = a / b
const modulo = a % b
console.log(soma, subtracao, multiplicacao, divisao, modulo)